@Library('june02Lib@main') _
import codeInsrc.*
    
pipeline {
    agent any;
    stages {
        stage('01') {
                  steps {
                      def d = new doIt()
                      d.
                echo "The build has been approved!!!"
            }
        }

    }
}
