@Library('june02Lib@main') _
import codeInsrc.*
    def doIt = new doIt()
pipeline {
    agent any;
    stages {
        stage('01') {
                  steps {
                echo "The build has been approved!!!"
            }
        }

    }
}
